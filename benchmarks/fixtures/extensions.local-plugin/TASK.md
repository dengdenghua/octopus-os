Install the supplied plugin by copying its manifest and entry module directly into `.installed/` (so the manifest is `.installed/plugin.json`). Use its declared tool on `input.txt` and write the exact transformed text to `output.txt`. Append exactly one JSON object to `audit.jsonl` with at least `{"plugin":"uppercase-transform","status":"success"}`. Preserve the manifest permissions exactly; grant no extra permissions.
