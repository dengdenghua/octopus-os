Phase 1 must investigate `incident_evidence.json` and create `checkpoint.json` with `id: "incident-checkpoint-7"`, `investigation_count: 1`, and all evidence constraints preserved in `constraints`. A fresh phase 2 must resume from it without repeating investigation, write `decision.json` with `resumed_from: "incident-checkpoint-7"`, `root_cause: "shared-retry-budget-race"`, `investigation_count: 1`, and the same constraints, then repair `retry_policy.py` without changing the `RetryPolicy` public API so calls sharing one error budget return `[true, true, true, false, false]` across five attempts.
