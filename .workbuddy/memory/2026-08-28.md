# 2026-08-28

## echo-os 项目全面分析（只读调研，未改代码）

### 关键结论：这是一个「去 fork 中途态」仓库
- 原为 echo-agent 的 fork，commit `60e835e refactor(os)!: P1 后端去 fork` 删掉 2216 文件（runtime/、tools/）
- **目录层 100% 完成，代码依赖层未完成**：appliance/ 仍有 20 处 `import runtime.*`，
  含 `task_projection.py:184` 的下划线私有模块 `_realtime_turn_lifecycle_resume`
- 真实分层：`entrypoint.py` 不组装 FastAPI app，只做凭据引导+降权后 `execvp("echo-agent")`；
  OS 通过 `ECHO_APP_EXTENSIONS=appliance.extension` 以扩展插件身份注入 agent 进程

### 实测数据（.venv 已装 echo-agent 0.2.0 + echo-agent-runtime 0.2.0）
- `pytest tests/appliance` → **883 passed, 1 skipped, 80s**，覆盖率 **79%**（CI 门槛仅 40%）
- `tests/` 根目录 407 个文件 / 12.4 万行是 agent 时代遗留，import runtime，
  实测**全部能跑通**（因为 venv 里有 agent wheel），但测的不是 OS 代码。
  pyproject `testpaths=["tests/appliance"]` 已绕过它们 —— 判断为「越权测试」而非「死测试」
- appliance/ 50 模块 10455 行，TODO/FIXME/注释代码块 **0 处**（有 lint 门禁），shell=True **0 处**

### 主要风险（按优先级）
- P0 `extension.py:171` / `device_link.py:201` 硬编码 token `"echo-device-link-managed"`，不可轮换
- P0 `security.py:42-43` jwt_secret 为空时返回 DEVELOPMENT_ACTOR 静默放行；
  与 `extension.py:110-111` 的 fail-closed 策略不一致
- P1 `docker_proxy.py:137-139` 未设 ECHO_DOCKER_PROXY_TOKEN 时鉴权直接 return True
- P1 `omv_router.py:311` 单函数 960 行；OMV 四件套 7375 行占 appliance 36%
- P2 `docker_proxy.py:62` 容器 id 双向前缀匹配过宽

### 文档与仓库卫生
- `CODE_WIKI.md` 1994 行、引用 runtime/ 44 次，**100% 描述 echo-agent，与本仓库无关**
  （且称前端是 Next.js 14 + React 18，实际是 Vite 7 + React 19 + Electron 34，无 Next.js）
- 仓库 5.3G：frontend 2.9G（release 1.5G + node_modules 1.4G）、packaging 967M、.git 375M
- .pak / node_modules / dist 均未被 git 跟踪（干净）
- 工作区未提交：1087 files changed, +30256 / -151594（净删 12 万行）
- 前端目录沿用 Next.js 的 src/app 约定但用 vite build，命名易误导

## 环境备注
- 项目 venv 在 `.venv`（python 3.12），已装 agent wheel，可直接跑 `pytest tests/appliance`
- macOS 无 `timeout` 命令，跑测试别加 `timeout` 前缀
