# 完成与视觉验收

完成一次 ComfyUI 任务至少需要：

1. 状态探测成功，工作流已读取；
2. 诊断中没有阻止运行的节点、输入或模型错误；
3. 队列返回 `prompt_id`，结果状态最终为完成；
4. 至少一个真实输出文件可访问，媒体类型和尺寸符合目标；
5. 查看输出并检查主体完整、身份和参考关系、构图、文字、边缘、手部、重复纹理、闪烁或时序破坏；
6. 报告使用的工作流 ID、关键输入、输出位置、仍存在的 warning 和可复现信息。

失败、取消、超时、空输出、文件不存在或只有结构 JSON 均不算完成。视频和音频工作流还必须检查时长、可解码性和声音轨道。
