apiVersion: v1
kind: Service
metadata:
  name: echo-agent
  namespace: echo-agent
  labels:
    app.kubernetes.io/name: echo-agent
spec:
  type: ClusterIP
  selector:
    app: echo-agent
  ports:
    - name: http
      port: 80
      targetPort: 8000
